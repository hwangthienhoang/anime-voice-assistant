export const VOICE_STATE_LABELS = Object.freeze({
  idle: 'Nhấn để nói',
  listening: 'Đang nghe…',
  thinking: 'Đang nghĩ…',
  speaking: 'Đang nói',
});

export const VOICE_STATES = Object.freeze(Object.keys(VOICE_STATE_LABELS));
